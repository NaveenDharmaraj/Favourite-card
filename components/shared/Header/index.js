import {
    Container,
  } from 'semantic-ui-react'
import '../Header/header.less';
const Header = (props) => (
    <Container>
        <h1>Header Component</h1>
    </Container>
);

export default Header;
